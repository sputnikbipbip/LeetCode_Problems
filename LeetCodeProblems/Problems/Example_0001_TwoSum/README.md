Example: Two Sum

Place each problem in its own folder under `Problems/` following the pattern `NNNN_Title`.

This example implements `IProblem` and can be run with:

```
dotnet run -- Example_0001_TwoSum
```
